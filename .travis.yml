language: c
services:
    - docker
sudo: required
before_install: 
    - make docker-build
script:
    - docker run -it comby-local-build:latest -e TRAVIS_JOB_ID="$TRAVIS_JOB_ID" /bin/bash -c "make && make clean && make build-with-coverage && make test && ./push-coverage-report.sh && ./benchmark"
